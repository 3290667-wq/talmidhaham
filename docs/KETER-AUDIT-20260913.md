# KETER-AUDIT-20260913 — אבחון בוט "כתר תורה" (קריאה בלבד)

**מועד חיתוך:** 2026-09-14 00:40 · **Timezone:** Asia/Jerusalem
**היקף:** אבחון בלבד. לא שונה קוד, DB, cron או פרודקשן; לא נשלחה הודעה לאף משתמש.
**ריצה כפולה:** נבדק במרשם המשימות — הרשומה היחידה ב-`running` היא הריצה הנוכחית
(`t-mu0e0jbi-5kwsk6`, startedAt 2026-09-13T22:30Z). אין תוצאות קודמות לשימוש חוזר.

---

## 1. זיהוי הפרויקט והפריסה

| | |
|---|---|
| נתיב | `/home/amichai/talmidhaham/bot` |
| ריפו | git מקומי, ענף `main`, HEAD `62f8e0f` (2026-08-21) |
| שירות | pm2 `talmid-bot`, `src/index.js`, online, uptime ~41h, restarts 0 |
| בריאות | `GET localhost:3005/health` → `{"ok":true,"group":"…@g.us"}` |
| מסלול הודעות | ווטסאפ → Evolution → `claude-bridge` (3002) → `talmid-bot` (3005) → `/send-text` |
| אחסון | `bot/data/store.json` (JSON, לא DB). 8 גיבויי `.bak` |
| AI | Gemini, ברירת מחדל `gemini-3.6-flash` (`src/ai.js`) |
| תלויות npm | **אפס** — Node בלבד |
| הוראות | `bot/README.md` בלבד. **אין CLAUDE.md ואין AGENTS.md** |
| תיעוד עמיחי | `תכנית-עבודה-תלמיד-חכם.pdf` + `plan.html` בשורש |

### ⚠️ ממצא מבני: הקוד החי אינו בגיט
`git status`: **12 קבצים משונים + 3 לא-מנוטרים**. מול HEAD: `10 files changed, 1581 insertions(+), 249 deletions(-)`.
הלא-מנוטרים כוללים את **`src/channels/bridge.js` (96 שורות — ערוץ הווטסאפ שבפרודקשן)**
ואת **`src/sefaria-books.js` (85 שורות — מסלול הספר החופשי)**. הקומיט האחרון קדם כמעט לכל
הפונקציונליות החיה. **ה-SHA אינו מתאר את מה שרץ**; אין rollback ואין היסטוריה לשינויים מאוגוסט.
`src/channels/greenapi.js` נמחק מעץ העבודה אך עדיין בגיט (ערוץ נטוש).

### ציר זמן לפי mtime
`fsrs.js` 08-02 · `bridge.js`/`sefaria-books.js`/`config.js`/`index.js`/`console.js`/`sefaria.js` 08-16 ·
`scheduler.js` 08-17 · `ai.js` 08-26 · `conversation.js`/`curriculum.js`/`db.js` 08-31.
**אין שינוי קוד מאז 2026-08-31.**

---

## 2. מסע המשתמש — מנגנונים קיימים (מקור לכל אחד)

| שלב | מימוש | מקור |
|---|---|---|
| הרשמה | פקודת `הרשמה` → שאלון: מסלולים, קצב, שעה, שבת | `conversation.js` |
| מסלולים | 4 מובנים (גמרא · משנה · שו"ע ב-4 חלקים · תנ"ך) + **מסלול 5: כל ספר בספריא** דרך `/api/name/` + `/api/shape/` עם הצעות תיקון לשגיאת כתיב | `curriculum.js`, `sefaria-books.js` |
| משימה יומית | `buildDaily(store)`; הודעת בוקר בשעה אישית | `conversation.js`, `scheduler.js` |
| לימוד | הבוט לא מלמד — הלומד לומד מהספר. `טקסט` שולף את המקור מספריא + קישור | `sefaria.js` |
| בחינה | `למדתי` → `generateQuiz` | `ai.js:133` |
| משוב | `gradeAnswer` → ציון 0-100 + משוב מילולי | `ai.js:176` |
| חזרה מרווחת | סולם `[1,3,7,14,30,90,180,365]`; ≥80 מקדם, <50 מוריד, 50-79 נשאר | `fsrs.js` |
| תזכורות | בוקר + כל 3 שעות מהפעילות האחרונה; שקט אחרי 22:00; שבת מושתקת מיום ו' 16:00 | `scheduler.js` |
| ניקוד | `streak` + `totalLearned` + `isOwned` (3 חזרות) | `fsrs.js`, `conversation.js:1525` |
| כפתורים | עד 3 לכל הודעה; `id` = הפקודה; נפילה לטקסט | `bridge.js` |
| שסתום קבוצתי | שותק על פטפוט, מגיב רק לפקודות | `conversation.js` |
| **ממשק מנהל** | **לא קיים** (grep ל-admin/dashboard/metric — אפס התאמות) | — |
| **פרסים** | **לא קיים** מעבר לרצף | — |

### מנגנונים שכבר פתורים — אין לבנות מחדש
- **שאלות כפולות:** `avoid[]` של שאלות קודמות + 6 `QUIZ_ANGLES` מתחלפות לפי `round` (`ai.js:119-165`).
- **ניחוש:** השאלות הן שליפה חופשית בטקסט, לא רב-ברירה. איסור מפורש על "נכון/לא נכון".
- **עיגון במקורות:** `IRON_RULES` — כל תשובה נבנית מטקסט ספריא בלבד; שאלת הלכה למעשה מופנית לרב.
- **ייחוס יחידה:** לכל שאלה שדה `unit` שחייב להתאים לכותרת המקור.

---

## 3. נתונים (מצטברים בלבד)

**מקור:** `bot/data/store.json` → `users[*].units / stats / daily`. אין DB ואין טבלאות.

| מדד | ערך |
|---|---|
| רשומים | **1** |
| פעילים 1 יום (אירוע לימוד אמיתי) | **0** |
| פעילים 7 ימים | **1** |
| פעילים 30 ימים | **1** |
| נרשמים חדשים ב-30 יום | **0** (המשתמש היחיד: `createdAt` 2026-08-02) |
| יחידות שנלמדו | 30 |
| ימי לימוד ייחודיים | 11 (16.8 → 8.9) |
| לימוד אחרון | **2026-09-08 — לפני 6 ימים** |
| שאלות שנוצרו | 150 (חציון 5 ליחידה, טווח 1-10) |
| יחידות עם שאלות מ-AI | 29 / 30 |
| ניסיונות (ציונים) | 49 |
| **דיוק ניסיון ראשון** | ממוצע **79.9**, חציון **87**, n=30 |
| **דיוק בחזרה** | ממוצע **82.4**, n=19 |
| חזרות שבוצעו ליחידה | 0 → 19 יח' · 1 → 4 · 2 → 6 · 3 → 1 |
| **יחידות בפיגור חזרה** | **30 מתוך 30 (100%)** |

**פילוח מסלולים:** משנה זבחים 10 · שמואל א׳ 10 · ראש השנה 6 · מסילת ישרים 3 · קיצור שו"ע 1.
**יציאה באמצע:** 5 מסלולים נפתחו, אף אחד לא הושלם; הפעיל כיום הוא ראש השנה (דף ה׳).

**פילוח לפי יום:** 08-16:1 · 17:4 · 19:1 · 20:3 · 25:4 · 26:1 · 28:1 · 30:7 · 31:4 · 09-06:3 · 09-08:1

### חסר — לא אפס
- **אין טלמטריה על הודעות יוצאות.** ה-scheduler אינו כותב לוג. אי אפשר לדעת אם הודעות הבוקר 9-13.9 נשלחו ונותרו ללא מענה, או לא נשלחו כלל.
- **אין מדידת עלות ואין ספירת טוקנים.**
- **אין שימור היסטורי:** `daily` נדרס כל יום; `stats.lastCompleted` תקוע ב-2026-08-02 בעוד `units.learnedAt` מגיע ל-08.09 → **המונה `streak` אינו אמין** (מציג 1).
- **זמני תגובה:** רק דגימה בלוג — `grade` ~1.8-1.9 שנ', טיפול בהודעה 0.5-4.3 שנ'. אין אגרגציה.
- **שגיאות:** `talmid-bot-error.log` = 1,007 בתים, אירוע יחיד — עומס רגעי ב-Gemini (429) במסלול `generateQuiz`. `out.log` לא נכתב מאז 12.09 07:01.

---

## 4. מכשולים לשיפור ידע — ראיות מול השערות

### ראיות (נמדדו)
1. **מנוע החזרה לא נצרך בפועל.** 100% מהיחידות בפיגור; 19/30 מעולם לא נחזרו. זהו לב המוצר.
2. **החזרות הן משיכה, לא דחיפה.** `dueUnits()` לא נקרא מה-scheduler — רק מהפקודה `חזרה` ומכפתור. `scheduler.js:53` מתנה תזכורת ב-`open.length` (משימת לימוד פתוחה); מי שסיים את הלימוד או שאין לו משימה — **לא מקבל שום תזכורת על 30 חזרות שבפיגור**.
3. **אין תקרה לתור החזרות** (`filterDue` מחזיר הכל, `conversation.js:1015`). "חזרה" היום פותח **30 יחידות / 150 שאלות** ברצף, שאלה-שאלה, כל אחת עם סבב Gemini. חומת כניסה שמונעת התחלה.
4. **הרצף נשבר ולא משוקם.** `streak=1` מול 11 ימי לימוד — אין מנגנון שמחזיר לומד שנשר (6 ימים ללא לימוד, אפס פנייה יזומה).
5. **דילול לרוחב:** 5 מסלולים נפתחו, 0 הושלמו.
6. **6 מתוך 30 ציוני ניסיון ראשון הם בדיוק 60** — חשד לברירת מחדל/עיגול של המדרג ולא להערכה אמיתית. דורש בדיקה.

### השערות (לא נמדדו)
- עומס תזכורות — לא ניתן לאמת בלי לוג שליחות.
- איכות השאלות במסלולים ארוכים (זבחים/שמואל) — לא נבדקה ידנית.
- האם `sentMorning` נכשל בשקט כשה-`send` נופל (הדגל נקבע **לפני** השליחה, `scheduler.js:44-47` — הודעה שנכשלה לא תישלח שוב).

### חסמי אמינות/אבטחה
- `bot/.env` בעץ העבודה עם מפתח Gemini; `.gitignore` מכסה — אומת שלא נכנס לגיט.
- `store.json` בלי הצפנה, מכיל שמות ומזהי ווטסאפ. אין rotation, גיבוי ידני בלבד.
- אין אימות על `POST` מהברידג' ל-3005 (מוגן רק ע"י localhost).
- כתיבה לא-אטומית ל-`store.json` — קריסה באמצע `save()` עלולה לאבד את כל ההתקדמות.

---

## 5. שיפורים מוצעים — לפי סדר עדיפות

| # | שיפור | למה | רכיב קיים לשימוש חוזר | בדיקה |
|---|---|---|---|---|
| 1 | **דחיפת חזרות ב-scheduler** — לשחרר את התנאי `open.length` ולשלוח תזכורת כשיש `dueUnits` גם בלי משימת לימוד | סוגר את הפער שגורם ל-100% פיגור | `dueUnits()`, `reminderPayload()`, `BTN.review` | להריץ `--console` עם store מזויף; לוודא שליחה אחת ליום לכל היותר |
| 2 | **תקרה לתור החזרות** (למשל 3 יחידות / 10 שאלות לסבב) + "המשך" | 150 שאלות = אפס התחלות | `filterDue()` — להוסיף `.slice()` | `test-offline.mjs` |
| 3 | **לוג שליחות + מונה יומי** (JSONL ליד `store.json`) | בלי זה אי אפשר לדעת אם הבוט מדבר | `console.log` הקיים ב-`bridge.js` | לבדוק שהקובץ נכתב אחרי מחזור בוקר |
| 4 | **תיקון `streak`/`lastCompleted`** — לעדכן מ-`units.learnedAt` ולא מנתיב נפרד | מונה שגוי שוחק אמון ומבטל את התמריץ היחיד | `stats` הקיים | השוואה מול 11 ימי הלימוד בנתונים |
| 5 | **החזרת הקוד החי לגיט** (קומיט של 15 הקבצים) + הסרת `greenapi.js` | אין rollback למה שרץ | — | `git status` נקי |
| 6 | **פנייה יזומה ללומד נושר** (אין לימוד 3+ ימים → הודעה אחת, לא חוזרת) | 6 ימי שקט מוחלט | `scheduler.js`, `send` | דגל `lastNudgeAt` למניעת הצפה |
| 7 | **כתיבה אטומית ל-`store.json`** (tmp+rename) | סיכון לאובדן כל הנתונים | `db.js:save()` | הרג תהליך באמצע שמירה |
| 8 | **בדיקת ציון 60** — לבדוק אם `gradeAnswer` נופל לברירת מחדל | 20% מהציונים זהים | `ai.js:176` | 10 תשובות ידניות מדורגות |
| 9 | **סגירת מסלול לפני פתיחת חדש** — אזהרה קיימת ב`שנה תכנית`, להרחיב להצעה אקטיבית | 5 פתוחים, 0 סגורים | האזהרה הקיימת | `test-offline.mjs` |

---

## 6. מה נבדק ומה לא

**נבדק:** מבנה הפרויקט · README · git ומצב הפריסה · pm2 · health · wiring לברידג' ·
כל 12 קובצי המקור (קריאה) · `store.json` באגרגציה · לוגי pm2 · חיפוש ממשק מנהל/טלמטריה.

**לא נבדק:** איכות תוכן השאלות ידנית · אימות שליחת הודעות בוקר בפועל (אין לוג) ·
עלות Gemini בפועל (אין מדידה, לא נגשתי לקונסולת Google) · התנהגות הבוט בקבוצה עם מספר
משתתפים (יש רק אחד) · `test-offline.mjs` לא הורץ (מריץ קוד, לא קריאה בלבד).

**לא נחשף:** שמות, טלפונים, מזהי ווטסאפ, מפתחות, תוכן שיחות אישי.
